{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions for the settings window",
  "windows": ["main"],
  "permissions": ["core:default"]
}

